<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hr.igord.data.CatDataMapper">
    <select id="selectCatData" resultType="hr.igord.data.CatData">
        select name catName from animal where animal_id = #{id}
    </select>
</mapper>